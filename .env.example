# MongoDB Configuration
MONGODB_URI=mongodb: your-url
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/codetracker

# Server Configuration
PORT=5000
NODE_ENV=development

# Codeforces API Configuration
CODEFORCES_API_BASE_URL=https://codeforces.com/api

# Email Configuration (Optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# JWT Secret (for authentication if needed)
JWT_SECRET=your-super-secret-jwt-key-here
